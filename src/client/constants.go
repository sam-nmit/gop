package client

const (
	gopStartProcess = "GOP.StartProcess"
	gopReadLog      = "GOP.ReadLog"
	gopStopProcess  = "GOP.StopProcess"
	gopList         = "GOP.List"
	gopStartService = "GOP.StartService"

	gopSetServiceEnabled = "GOP.SetServiceEnabled"
	gopRemoveService     = "GOP.RemoveService"
)
